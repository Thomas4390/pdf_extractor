# Core application dependencies for Streamlit Cloud deployment
# Only include actual Python packages needed by the application

# Streamlit web framework
streamlit>=1.30.0

# Data manipulation and analysis
pandas>=2.0.0
openpyxl>=3.1.0  # Excel export support for pandas

# PDF processing (PyMuPDF)
PyMuPDF>=1.23.0

# Pydantic for data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client for API calls (Monday.com, OpenRouter)
httpx>=0.25.0

# YAML parsing for prompts
PyYAML>=6.0.0

# Environment variable management
python-dotenv>=1.0.0

# Google Sheets integration for advisor data sync
gspread>=5.12.0
google-auth>=2.23.0
